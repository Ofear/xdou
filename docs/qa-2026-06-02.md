# xdou productivity hardening QA — 2026-06-02

## Verdict

Local xdou is now cockpit-productivity hardened and validated. The operator can stay inside `xdou cockpit` for the core loop:

`ask/find/web -> plan/code -> inspect diff/review/status -> rerun tests -> fix -> apply -> undo/discard -> continue`

## Shipped in this pass

- Cockpit-native commands:
  - `/test [run-id]`
  - `/fix [run-id]`
  - `/discard [run-id]`
  - `/undo [run-id]`
- Hotkeys:
  - `t` rerun tests
  - `f` fix selected run blockers
  - `d` discard selected run worktree
  - `u` undo selected applied patch
- CLI commands:
  - `xdou test [run-id] [--json]`
  - `xdou discard [run-id] [--json]`
  - `xdou undo [run-id] [--json]`
- Runtime command safety:
  - validation refuses destructive scripts before execution
  - blocks patterns such as `rm -rf`, `git reset --hard`, `git clean`, `git push`, and pipe-to-shell installers
- Apply lifecycle hardening:
  - discard removes the worktree but preserves run audit artifacts
  - undo reverses an applied run patch and records `undo-result.json`
  - rerun validation records `validation-rerun.json` and timeline evidence

## Validation evidence

Passed locally:

- `npm run typecheck`
- `npm test -- --run`
  - 22 test files passed
  - 60 tests passed
- `npm run build`
- `npm pack --dry-run`
  - package: `@ofear/xdou@1.2.3`
  - package size: 66.8 kB
  - unpacked size: 287.0 kB
  - shasum: `73859f20f6b492ca6432039b1d4bd9a0e95b5667`
- `npm run lint`
- built CLI smoke:
  - `node dist/cli.js help`
  - `node dist/cli.js cockpit --snapshot`

## External blocker

Publishing/global npm update is blocked by npm auth on this machine:

`npm whoami` returned `E401 Unauthorized`.

Local built artifact is good. Registry publishing still requires npm login/token outside the codebase.
